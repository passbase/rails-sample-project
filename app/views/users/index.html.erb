<h1>All Users</h1>

<%= button_to  "Add New User", new_user_path, method: :get  %>
<h3>User List:</h3>
<ul>
    <% @users.each do |user|%>
    <li><%= link_to user.name, user_path(user)%></li>
    <%end%>
</ul>